import tkinter as tk
import socket


window = tk.Tk()
window.geometry()
window.title("Python-Messager Info")
Main_Frame = tk.Frame(window)
settings_frame = tk.Frame(window)
settings_button = tk.Button(Main_Frame, text="Settings", width=10, height=3)
